# Pure-Java-Bitcoin-Wallet-CLI-
A fully functional, zero-dependency Bitcoin wallet implementation written in pure Java. This project implements core cryptographic primitives (SHA-256, RIPEMD-160, Secp256k1) and wallet standards (WIF, P2PKH, Base58Check) entirely from scratch using the standard JDK.
